spring:
  datasource:
    url: jdbc:mysql://localhost:3306/patient_data
    username: root
    password: manager
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mysql:
    datasource:
      url: jdbc:mysql://localhost:3306/patient_data
      username: root
      password: manager
